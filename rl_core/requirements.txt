# CPU-only PyTorch wheels: this app targets small models (classic control,
# AlphaZero on tiny boards) where CUDA isn't needed, and the CPU wheel is a
# fraction of the size of the default CUDA-bundled build — much faster to
# install for the typical desktop user. GPU users can flip the "GPU" switch
# in Settings instead, which installs from requirements-gpu.txt (CUDA build)
# and applies to this same env.
# (--index-url replaces the default so torch resolves to the CPU wheel;
# --extra-index-url falls back to PyPI for every other package below.)
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

numpy>=1.26
torch>=2.2
# [box2d,mujoco,atari]: unlocks LunarLander/BipedalWalker/CarRacing, the 11
# MuJoCo robotics tasks, and the ~40 Atari games in the Environments gallery.
# All three ship prebuilt wheels (no swig/compiler needed) and add well under
# 100MB combined, so it's worth including by default rather than leaving
# most of the gallery greyed out until a user manually installs extras.
gymnasium[box2d,mujoco,atari]>=0.29
stable-baselines3>=2.3
pillow>=10.0
# gymnasium.wrappers.ResizeObservation (Resize Observation in the wrapper
# catalog — see rl_core/envs/wrappers.py) shells out to OpenCV to resize
# frames. Listed explicitly rather than relying on some other dependency
# pulling it in transitively — headless build since this never touches a
# display/GUI.
opencv-python-headless>=4.8
# MiniGrid (gridworld navigation/exploration/memory tasks) and Highway-env
# (autonomous-driving decision-making) — both pure-Python, prebuilt-wheel
# packages (no swig/compiler, like the box2d/mujoco/atari extras above),
# unlocking the "minigrid"/"highway_env" categories in the Environments
# gallery (see rl_core/envs/minigrid_envs.py / highway_envs.py).
minigrid>=3.0
highway-env>=1.10
# Gymnasium-Robotics (rl_core/envs/robotics_envs.py) — AntMaze/PointMaze
# goal-conditioned maze navigation. Pure-Python wheel itself (no
# swig/compiler); its own dependencies (PettingZoo core, Jinja2, imageio)
# are the same. Only actually *uses* the `mujoco` extra already pulled in
# above by `gymnasium[...,mujoco,...]` — nothing extra to compile.
gymnasium-robotics>=1.4
# PettingZoo (rl_core/envs/pettingzoo_envs.py) — the "marl" category's 5
# built-in PettingZoo benchmarks (3 MPE envs via mpe2, SISL's Pursuit, and
# Butterfly's Knights Archers Zombies). `pettingzoo` itself is already
# pulled in transitively by gymnasium-robotics above; pinned explicitly
# here (with the `[butterfly]` extra, for KAZ's pygame-ce rendering/step
# loop) since these envs now depend on it directly, not just through
# another package's internals.
# `mpe2` is PettingZoo's own actively-maintained fork of its old built-in
# `pettingzoo.mpe` module (deprecated/removed upstream) — pure-Python,
# no swig/compiler, same "prebuilt wheel" story as everything else above.
# Pursuit (`pettingzoo.sisl`) needs no extra dependency beyond `pettingzoo`
# itself (unlike `multiwalker`/`waterworld` in the same `sisl` family,
# which need Box2D/pymunk — not installed, and not one of the envs used).
pettingzoo[butterfly]>=1.24
mpe2>=1.1
# RWARE + LBForaging (rl_core/envs/tuple_marl_envs.py) — the "marl"
# category's 4 "big" Tuple-space benchmarks (2 RWARE warehouse-robot
# sizes + 2 Level-Based Foraging sizes) alongside the PettingZoo ones
# above. Both are pure-Python, no compiler/game client needed; both only
# fail to render a GIF preview headlessly (no display -> caught and
# skipped, see `TupleMarlVectorEnv.render` — training itself is unaffected).
rware>=2.0
lbforaging>=2.0
# SMAClite (rl_core/envs/tuple_marl_envs.py's `smac_*` scenarios) is
# deliberately NOT pinned here, unlike everything above — its own
# setup.py hard-pins `Rtree==1.0.0`, which has no prebuilt wheel past
# Python 3.10 (only a source dist needing a system `libspatialindex` at
# import time), so adding it directly here would break `pip install -r`
# for anyone on a newer interpreter without that system library already
# present. It's installed on demand instead (2-step command, see
# `EXTRA_HINT['smaclite']` in `src/pages/Environments.tsx`) exactly like
# `nle`/`minihack`/`highway-env` above already are for their own reasons.
# NetHack Learning Environment / MiniHack (rl_core/envs/nethack_envs.py) —
# NOT installed by default, unlike every other extra above: `nle` only
# ships prebuilt wheels for manylinux (Linux x86_64/aarch64), so on macOS/
# Windows `pip install nle` falls back to building the NetHack C source
# from a tarball (needs cmake/flex/bison/a C compiler) — exactly the heavy,
# failure-prone install this file otherwise avoids. Uncomment on Linux, or
# install manually, to unlock the "nethack" category in the gallery.
# nle>=1.1
# minihack>=1.0
# minihack's own code still does `import pkg_resources` (minihack/base.py)
# — an API setuptools has fully removed as of 81.0.0 (Nov 2025), so
# whatever plain `pip install minihack` resolves setuptools to transitively
# (nothing pins an upper bound) breaks the import with a bare
# `ModuleNotFoundError: No module named 'pkg_resources'` the moment you
# actually try to use it. Not our bug to fix (minihack's, upstream), but
# pinning below is what makes `pip install -r` actually leave a *working*
# minihack instead of a "looks installed, silently broken until first
# import" one — uncomment alongside the two lines above.
# setuptools<81
